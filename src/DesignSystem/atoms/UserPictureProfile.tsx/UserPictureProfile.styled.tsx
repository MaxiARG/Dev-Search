import { styled, XStack } from 'tamagui';

const UserPictureProfile = styled(XStack, {
  borderRadius: '$radius.md',
  width: '$size.xxl',
  backgroundColor: '$color.surface',
});

export default UserPictureProfile;
